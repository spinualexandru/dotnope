'use strict';

const {
    enableStrictEnv,
    disableStrictEnv,
    getAccessStats,
    isEnabled,
    hasNativeModule
} = require('./lib/dotnope');

module.exports = {
    enableStrictEnv,
    disableStrictEnv,
    getAccessStats,
    isEnabled,
    hasNativeModule
};
